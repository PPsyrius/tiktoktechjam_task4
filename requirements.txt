# Core test dependency (also listed in pyproject.toml [dependency-groups.tests])
# Keep stdlib-only runtime; this file is for `pip install -r requirements.txt`
pytest>=8,<10
numpy>=1.26,<3; python_version < "3.12"
numpy>=2.5,<3; python_version >= "3.12"
sentence-transformers>=3,<7
